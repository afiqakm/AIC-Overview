import MaterialCard from '../components/atoms/MaterialCard';

type Props = {};

const SciencePark = ({}: Props) => {
    return (
        <div>
            <MaterialCard label='Originium' />
        </div>
    );
};

export default SciencePark;
